{
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/ex1",
            "args": [],
            "cwd": "${workspaceFolder}",
            // 注意了，这个关联tasks.json
            "preLaunchTask": "build and run"
        }
    ]
}